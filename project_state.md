# 计划分解器 - 项目状态与任务追踪

> 项目采用分步确认机制。以下每个**阶段**和**子任务**在执行前都需要与用户确认。
> 当前状态：✅ Step 1–7 / Step 9 已完成；🔶 Step 10 部署上线进行中（GitHub 仓库已建、代码已提交本地，待用户在自有网络 `git push`；Vercel 待导入）；下一步：Step 8 AI 服务接入（可选）

---

## 项目总览

| 项目 | 内容 |
|------|------|
| 产品名称 | 计划分解器（Plan Decomposer） |
| 当前阶段 | Step 9：测试/优化/收尾（已完成）｜ 下一步：Step 8 AI 服务接入（可选） |
| 已完成 | PRD、架构文档、项目状态文档 |
| 待办 | 见下方阶段列表 |

---

## 阶段规划

### Step 1：项目基础与设计系统搭建
**目标**：建立项目骨架、HTML 基础结构、CSS 设计令牌与基础样式

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 1.1 创建 index.html 与基础结构 | ✅ 已完成 | 创建三栏布局的 HTML 骨架 |
| 1.2 创建 CSS 设计令牌（tokens.css） | ✅ 已完成 | 定义颜色、字体、间距、阴影等变量 |
| 1.3 创建基础样式（base.css） | ✅ 已完成 | CSS reset、body 样式、通用工具类 |
| 1.4 创建布局样式（layout.css） | ✅ 已完成 | 三栏布局、面板尺寸、滚动行为 |
| 1.5 创建组件基础样式（components.css） | ✅ 已完成 | 按钮、输入框、卡片、时间块基础样式 |

**阶段确认问题**：
1. 这个阶段的任务范围是否符合你的预期？
2. 是否需要我先输出一个静态的视觉预览再进入 Step 2？

---

### Step 2：数据层与工具函数
**目标**：建立状态管理、数据持久化和通用工具函数

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 2.1 实现 utils.js | ✅ 已完成 | 时间解析/格式化、ID 生成、日期计算 |
| 2.2 实现 store.js | ✅ 已完成 | 状态管理 + 存储抽象层（接口隔离，底层先用 localStorage） |
| 2.3 设计数据结构 | ✅ 已完成 | Task / Step / ScheduleItem 数据模型（不预置示例数据） |
| 2.4 添加数据版本控制 | ✅ 已完成 | 便于后续 localStorage / 后端数据迁移 |
| 2.5 面板布局配置持久化 | ✅ 已完成 | 保存三栏顺序与宽度，供 Step 2.5 调节功能消费 |

**阶段确认问题**：
1. 示例数据：已确认**不需要**，首次打开为空状态（时间刻度仍展示）。
2. 持久化策略：建议「存储抽象层」——先用 localStorage，公开时仅替换底层适配器为后端 API，业务代码不动。是否认可？
3. AI 分解：mock 完成界面，真实 API 留 Step 8（已确认）。

---

### Step 2.5：面板布局可调节（宽度 / 顺序）✅ 已完成
**目标**：让用户自主拖拽调节三栏宽度，并拖拽重排三栏顺序，配置持久化

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 2.5.1 实现宽度拖拽调节（resizer） | ✅ 已完成 | 面板间灰色分隔区拖拽改变列宽，最小 240px，实时生效 |
| 2.5.2 实现顺序拖拽重排 | ✅ 已完成 | 顶部三灰点 drag handle 按住拖拽换位，键盘方向键也可 |
| 2.5.3 布局配置持久化 | ✅ 已完成 | 顺序与宽度写入 store -> localStorage（Step 2 已建模型） |
| 2.5.4 响应式下行为 | ✅ 已完成 | 窄屏自动堆叠，禁用宽度拖拽、顺序仍可调 |

**阶段确认问题**：
1. 宽度调节是否需要最小/最大限制（例如每栏最小 240px）？
2. 顺序重排用「拖拽」还是「提供左右切换按钮」更合你意？
3. 调节后是否允许「一键复位默认布局」？

---

### Step 3：左侧 AI 任务分解面板 ✅ 已完成
**目标**：实现用户输入、AI 分解、步骤列表展示与基础编辑

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 3.1 渲染输入区域 | ✅ 已完成 | 多行文本框、AI 分解按钮（无清空按钮），空输入校验 |
| 3.2 实现 ai-service.js（mock 模式） | ✅ 已完成 | 关键词模板库，逐条返回步骤 + AI 推荐时长（estimatedMinutes） |
| 3.3 渲染步骤列表 | ✅ 已完成 | 序号、标题、AI 建议时长胶囊、底部「重新生成全部」 |
| 3.4 实现步骤基础操作 | ✅ 已完成 | 删除、行内编辑标题（点击/Enter 保存/Esc 取消）、上移/下移 |
| 3.5 添加加载与空状态 | ✅ 已完成 | AI 生成中 Loading（模拟异步）、无步骤 Empty State |

**阶段确认问题（已据用户回复定稿）**：
1. 时长：AI 分解时逐条推荐每步时长并直接写在卡片上（mock 用关键词模板给出合理时长，真实 API 留 Step 8）。
2. 步骤卡操作：删除、编辑标题、调整顺序（上移/下移）已支持；**不使用**统一默认 30 分钟。
3. 重新生成：不放单卡，置于步骤列表**最底部**「重新生成全部」按钮，仅重跑当前整份计划（带二次确认，避免误触丢失）。

---

### Step 4：中间时间日历面板 ✅ 已完成
**目标**：实现时间线渲染、日期切换、当前时间指示

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 4.1 渲染时间线刻度 | ✅ 已完成 | **00:00–24:00** 全天刻度，每小时实线 + 半小时虚线（空状态也展示） |
| 4.2 渲染日期标题与切换 | ✅ 已完成 | 当前日期、前一天/后一天/回到今天（store 驱动，打开时自动滚动到当前时刻） |
| 4.3 渲染当前时间线 | ✅ 已完成 | 红色横线，每分钟自动移动（setInterval 60s） |
| 4.4 实现时间块渲染 | ✅ 已完成 | 根据 ScheduleItem 计算位置与高度（高度 = AI 推荐时长，Step 5 拖拽生成时确定） |
| 4.5 时间块视觉状态 | ✅ 已完成 | 默认、悬停、选中、键盘可达（Enter/空格选择） |

**阶段确认问题（已据用户回复定稿）**：
1. 时间线范围：**00:00–24:00** 全天（用户确认，原 06:00-23:00 调整）。
2. 时间块默认时长 = 步骤 AI 推荐时长（estimatedMinutes），Step 5 拖拽生成时按此换算高度。
3. 单日视图（Step 4 仅做单日；多天视图未排期）。

---

### Step 5：拖拽系统 ✅ 已完成
**目标**：实现左侧步骤 ↔ 日历的跨面板拖拽，及步骤状态（待规划/已安排/已完成）可视化

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 5.1 左侧步骤卡片可拖拽 | ✅ 已完成 | draggable=true（已完成步骤锁定不可拖） |
| 5.2 时间线放置区 | ✅ 已完成 | dragover/drop 处理，落点 5 分钟吸附，虚线预览线 |
| 5.3 拖拽时视觉反馈 | ✅ 已完成 | 时间线/左侧面板高亮落点、块半透明拖影 |
| 5.4 时间块垂直拖拽调时间 | ✅ 已完成 | 拖日历内块改起始时间（moveSchedule，保持时长） |
| 5.5 左→日历生成时间块 | ✅ 已完成 | 起始=落下位置、时长=AI 推荐（已排则删旧建新=移动） |
| 5.6 时间吸附逻辑 | ✅ 已确认 | 按 **5 分钟**吸附（utils.js snap 默认改 5） |
| 5.7 重叠策略 | ✅ 已确认 | **允许同一时间段多个任务重叠** |
| 5.8 日历块拖出左侧=取消安排 | ✅ 已完成 | 拖块到左侧面板 → deleteSchedule，左卡变回「待安排」 |
| 5.9 完成态（灰+划线+锁定） | ✅ 已完成 | 步骤卡/时间块勾选完成 → 两侧同步灰化划线，不可再拖 |

**交互逻辑（用户澄清定稿）**：
1. AI 分解后步骤以「待规划」状态逐条展示在左栏。
2. 拖步骤入日历 → 生成时间块，左侧该卡变「已安排」（墨绿描边+浅底）。
3. 拖日历块到左侧面板 → 取消安排，左卡变回「待安排」。
4. 勾选完成 → 步骤与对应时间块同步灰化+删除线，**无法再次拖动**。
5. 允许重叠；落点起始=放下位置，时长=AI 推荐；5 分钟吸附。
6. **不做**「拖出即删除任务」——拖出仅取消安排；删除任务仍用卡片上的 ✕。

---

### Step 6：右侧任务详情面板
**目标**：实现选中任务后展示和编辑详情

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 6.1 渲染默认空状态 | ✅ 已完成 | 未选中时提示（复用 .empty-state） |
| 6.2 渲染任务详情 | ✅ 已完成 | 标题、父任务、状态徽标、开始/结束/时长、描述、完成勾选 |
| 6.3 实现详情编辑 | ✅ 已完成 | 标题 + 描述（textarea）行内可编辑、即时自动保存 |
| 6.4 保存策略 | ✅ 已完成 | **即时自动保存**：输入即写回 store 并同步日历/左栏，无保存/取消按钮 |
| 6.5 删除该安排 | ✅ 已完成 | **删除步骤本身**：排期 + 左侧步骤一并移除（不可恢复），按钮加重置二次确认 |

**已确认决策（2026-07-22 用户拍板）**：
1. 编辑保存方式 = 即时自动保存（非显式保存按钮）
2. 选中来源 = 左侧步骤卡点击 **与** 日历时间块点击 都能打开详情（新增 `step:select` 事件接线；无排期显示「未安排时间」+「安排到今天」入口）
3. 详情面板「删除该安排」= 删除步骤本身（sync 删除 schedule + step），需二次确认防误删
4. 数据模型：step 需新增 `description` 字段（mock 返回与 store 写入同步带上）
5. 完成勾选与日历双向同步（复用 `toggleStepComplete` + `is-done` 灰化锁定）

---

### Step 7：顶部标题栏与全局功能 ✅ 已完成
**目标**：实现顶部栏计划名展示、导出当前计划为 JSON、响应式基础适配

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 7.1 顶部栏计划名展示 | ✅ 已完成 | 顶部栏中央显示当前计划名（超长省略）；无计划显示「尚未创建计划」；导出按钮无计划时禁用 |
| 7.2 导出当前计划为 JSON | ✅ 已完成 | 新建 `js/export.js`：导出当前计划（标题/描述/步骤含子任务与完成态/排期），含 app、schemaVersion、exportedAt 元信息；Blob 触发下载，文件名带计划名+日期；含手动编辑；成功后按钮短暂显示「已导出 ✓」 |
| 7.3 响应式适配基础 | ✅ 已完成 | 强化 `@media(max-width:768px)`：顶栏可换行、计划名转整行左对齐、日历堆叠后 `min-height:70vh` 保证可滚动；新增 `@media(max-width:420px)` 收起品牌标题 |

**已确认决策（2026-07-22 用户拍板）**：
1. 导出范围 = **当前计划**（非完整应用状态、非单日排期）。
2. **暂不需要导入**功能（导入留待后续）。
3. 导出内容**全部包含**手动编辑（标题、描述、子任务及其勾选、排期）。
4. 导出格式 = JSON（无图片导出）。

---

### Step 8：AI 服务接入（可选后期）
**目标**：将 mock AI 替换为真实 AI API

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 8.1 设计 AI Prompt | ⏳ 待确认 | 让 AI 返回结构化步骤数据 |
| 8.2 实现 API 调用 | ⏳ 待确认 | fetch 调用大模型接口 |
| 8.3 结果解析与错误处理 | ⏳ 待确认 | 解析 JSON、失败重试 |
| 8.4 API Key 管理 | ⏳ 待确认 | 环境变量或用户输入 |

**阶段确认问题**：
1. 你是否有偏好的 AI 服务提供商？
2. API Key 如何管理？
3. 是否需要流式输出效果？

---

### Step 9：测试、优化与收尾 ✅ 已完成
**目标**：确保功能稳定、视觉一致、代码可维护

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 9.1 功能自测 | ✅ 已完成 | 静态走查完整流程（输入→分解→编辑/排序→拖入排期→单排程→跨日期状态→详情/子任务→导出→刷新恢复），各模块接线一致、无断链 |
| 9.2 浏览器兼容性检查 | ✅ 已完成 | 纯原生 ES Modules + 标准 DOM/CSS，无实验性 API；环境无无头浏览器，未跑自动截图 |
| 9.3 可访问性检查 | ✅ 已完成 | 新增 `prefers-reduced-motion` 降级；新增 `.time-block:focus-visible` 焦点环；`--color-text-muted` 加深至 slate-600（对比度≥7:1，达 WCAG AA）；键盘可达（面板方向键、时间块 Enter/空格） |
| 9.4 代码整理与注释 | ✅ 已完成 | 清理 panel-resize.js 死代码（空 `if(isStacked){}`）；仅保留 2 处合法 `console.warn` 错误处理 |
| 9.5 生成 README | ✅ 已完成 | 新建 README.md：简介/特性/快速开始/使用流程/结构/技术栈/持久化/导出格式/键盘/路线图 |

---

### Step 10：GitHub 同步 + Vercel 部署上线 🔶 进行中
**目标**：代码上 GitHub 公开仓库，经 Vercel 托管让所有人可访问（纯静态站点，无构建步骤）

| 子任务 | 状态 | 说明 |
|--------|------|------|
| 10.1 初始化本地仓库 | ✅ 已完成 | `git init -b main`；新增 `.gitignore`（排除 .workbuddy/ 等）；初始提交 v1.2（含源码+README+PRD/架构文档） |
| 10.2 创建 GitHub 仓库 | ✅ 已完成 | `gh repo create plan-decomposer --public`，地址 https://github.com/qianhongrui2006-code/plan-decomposer （公开） |
| 10.3 推送代码到 GitHub | 🔶 待用户在自有网络执行 | 当前运行沙箱无法访问 github.com:443（git/curl 均超时），但 gh API 通道放行故仓库已建。用户在本机该目录执行 `git push -u origin main` 即可（本地已提交+remote 已配） |
| 10.4 Vercel 导入部署 | 🔶 待用户操作 | vercel.com 用 GitHub 登录 → New Project → Import `plan-decomposer` → Framework 选 Other / 无构建 → Deploy。后续每次 `git push` 自动重新部署 |
| 10.5 自定义域名（可选） | ⏳ 待定 | Vercel 项目设置里绑定自有域名 |

---

## 当前决策待确认

1. **技术栈**：采用 HTML + 原生 JS + CSS，不使用 React/Vue（已确认并已在 Step 1-3 落地）。
2. **AI 模式**：mock 完成界面，真实 API 留 Step 8（已确认）。
3. **时间线范围**：**00:00 - 24:00** 全天（用户确认，Step 4 已落地）。
4. **拖拽重叠**：已确认**允许**同一时间段多个任务重叠（Step 5 先做重叠）。
5. **主题方案**：已确认仅单一浅色主题，不做深浅切换。
6. **示例数据**：已确认**不需要**，首屏为空状态。
7. **面板可调**：三栏宽度可拖拽调节、顺序可拖拽重排（Step 2.5 已完成）。
8. **持久化策略**：已确认存储抽象层 + localStorage（自用）→ 后端 API（公开）。
9. **面板顺序重排**：每个模块顶部**居中横排**三个灰色小圆点 drag handle，按住拖拽换位（已确认，Step 4 顺带把灰点从竖排左侧改为横排顶部居中）。
10. **宽度调节**：鼠标移到模块间灰色分隔区拖拽；最小宽度默认 240px（已确认）。
11. **左栏步骤时长**：AI 分解时逐条推荐并在卡片展示；**不做**统一默认时长。
12. **重新生成**：置于步骤列表底部「重新生成全部」，仅重跑当前整份计划（已确认）。
13. **单计划模型**：左栏始终维护一份当前任务（replaceCurrentTask 替换并清空旧日程）。
14. **拖拽精度**：Step 5 日历拖拽按 **5 分钟**吸附（utils.js `snapMinutes` 默认改 5）。
15. **落块规则**：Step 5 从左侧拖步骤到时间线时，**起始时间取放下位置、时长取该步骤 AI 推荐时长（estimatedMinutes）**。
16. **面板可调体验修正**：用户反馈 Step 2.5 顺序/宽度用不了 —— 重写为「拖到目标上方高亮落点 + 松手交换（FLIP 动画）」；宽度分隔条加 `<||>` 抓取提示与 col-resize 光标。注意：第二次仍反馈用不了，**最终改为几何判定落点（游标 X 与各面板矩形比对），彻底弃用 elementFromPoint + pointer-events 穿透方案**，现已稳定。
17. **步骤三态**：待规划（默认）/ 已安排（拖入日历后左卡变色）/ 已完成（勾选后两侧灰化划线且锁定）。
18. **拖出行为**：日历块拖到左侧面板 = 取消安排（左卡回「待安排」）；**不做**拖出即删任务，删除仍用卡片 ✕。
19. **完成态同步**：步骤.completed 翻转时同步其所有日程.completed，左卡与日历块一并灰化划线。

---

## 变更日志

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-21 | v0.1 | 初始 PRD、架构、任务规划文档 |
| 2026-07-21 | v0.2 | Step 1 完成：index.html 三栏骨架 + tokens/base/layout/components 四套 CSS + app.js 脚手架 |
| 2026-07-21 | v0.3 | 视觉调整：主色改墨绿、删除「清空」按钮、板块改为圆角独立卡片 |
| 2026-07-21 | v0.4 | 规划调整：新增 Step 2.5 面板可调、Step 2 纳入布局配置持久化、去掉示例数据、修正 3.1 |
| 2026-07-21 | v0.5 | Step 2 完成：utils.js + store.js（存储抽象层/localStorage/CRUD/版本控制/布局配置）+ app.js 接入 store |
| 2026-07-21 | v0.6 | Step 2.5 完成：panel-resize.js（宽度拖拽/三灰点换位/键盘可达/持久化）、layout 改 flex、store 面板标识改身份键 task/calendar/detail、HTML 加 drag handle |
| 2026-07-21 | v0.7 | Step 3 完成：ai-service.js（mock 关键词分解+推荐时长）、task-panel.js（生成/渲染/行内编辑/上移下移/删除/底部重新生成全部）、store 增 replaceCurrentTask+moveStep、components.css 增强步骤卡、app.js 接入 |
| 2026-07-21 | v0.8 | Step 4 完成：calendar.js（00:00-24:00 刻度 + 当前时间红线每分钟移动 + 时间块渲染 + 日期导航）、拖拽手柄改为顶部居中横排三灰点、时间线范围改全天、PRD 同步 |
| 2026-07-21 | v0.9 | 面板可调重写（修复用户反馈「用不了」）：顺序拖拽改落点高亮+松手交换+FLIP动画；宽度分隔条加 `<||>` 抓取提示与 col-resize 光标；utils.js snap 改 5 分钟；记录 Step 5 决策（允许重叠/5分钟吸附/落点取放下位置+AI推荐时长） |
| 2026-07-21 | v1.0 | 修复三问题：①完成态不同步——calendar.scheduleSig 纳入 completed 字段，勾选后日历块正确变灰锁定；②时间块相邻无间隔——renderBlocks 上下各内缩 --block-gap(8px) 留缝；③苹果毛玻璃——body 加彩色光晕渐变底，面板/顶栏改 translucent + backdrop-filter blur；时间块去掉左侧深色条边、整条统一墨绿、选中用环形描边 |
| 2026-07-21 | v0.10 | 面板可调二次修复：重写 panel-resize.js 改用几何判定落点（弃用 elementFromPoint）；Step 5 完成：新增 drag-drop.js（步骤↔日历拖拽）、store 增 completed/toggleStepComplete/moveSchedule/isStepScheduled/getScheduleForStepOnDate；task-panel 步骤卡三态+完成勾选；calendar 块可拖拽+完成态；components.css 增补状态与落点样式 |
| 2026-07-21 | v0.11 | 体验微调：时间块最小高度 18→52px（短任务文字完整可见）；时间线小时高度 52→64px、块间隔 8→10px（更舒展）；步骤卡状态徽标锁定 nowrap+flex:0 0 auto，长时长不再挤换行 |
| 2026-07-22 | v1.1 | Step 7 完成：顶部栏显示当前计划名（无计划禁用导出）；新增 js/export.js 导出当前计划为 JSON（含步骤/子任务/排期/元信息，含手动编辑）；强化响应式（768px 堆叠+日历 min-height:70vh、420px 收起品牌标题） |
| 2026-07-22 | v1.2 | Step 9 收尾：静态走查全流程（无断链）；新增 `prefers-reduced-motion` 降级与 `.time-block:focus-visible` 焦点环；`--color-text-muted` 加深至 slate-600 达 WCAG AA；清理 panel-resize 死代码；新增 README.md |
| 2026-07-22 | v1.3 | Step 10 部署：git init(main)+.gitignore+初始提交 v1.2；`gh repo create plan-decomposer --public` 已建仓（https://github.com/qianhongrui2006-code/plan-decomposer）；推送因沙箱网络封禁 github.com:443 未能自动完成，待用户在自有网络 `git push -u origin main`；Vercel 导入步骤已就绪 |
