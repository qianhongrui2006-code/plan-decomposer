# 计划分解器 - 产品需求文档（PRD）

## 1. 产品概述

### 1.1 产品名称
计划分解器（Plan Decomposer）

### 1.2 产品定位
一款基于 AI 的智能计划分解与时间管理网页工具。用户输入一个宏观计划，AI 自动将其拆解为可执行的细化步骤，用户可通过拖拽将步骤安排到日历时间线上，并在右侧查看每个步骤的详细执行标准。

### 1.3 目标用户
- 需要将复杂项目拆解为可执行任务的个人用户
- 希望进行时间块管理的学习者/工作者
- 需要可视化时间安排的用户

### 1.4 核心价值
- **AI 智能拆解**：降低任务规划门槛
- **可视化排期**：拖拽式时间管理
- **标准明确**：每个步骤都有清晰的执行标准

---

## 2. 功能需求

### 2.1 整体布局
页面采用经典的三栏布局：

| 区域 | 位置 | 功能 |
|------|------|------|
| 左侧栏 | 左 | AI 任务分解面板 |
| 中间栏 | 中 | 时间日历/时间线面板 |
| 右侧栏 | 右 | 任务详情面板 |
| 顶部 | 上 | 页面标题与全局操作 |

### 2.2 左侧：AI 任务分解面板

#### 2.2.1 输入区域
- **任务描述输入框**：多行文本框，支持用户输入计划/任务描述
- **示例提示**：placeholder 显示"例如：我想在两周内学会 Vue3 基础"
- **AI 分解按钮**：点击后调用 AI 将任务分解为多个步骤
- **清空按钮**：清空当前输入

#### 2.2.2 生成步骤列表
- 以卡片/列表形式展示 AI 生成的步骤
- 每个步骤显示：
  - 步骤序号（Step 1, Step 2...）
  - 步骤标题
  - 预估时长
  - 拖动抓手图标
- 步骤支持拖拽到中间时间线
- 支持删除单个步骤
- 支持重新生成全部步骤

#### 2.2.3 步骤编辑
- 点击步骤可编辑标题和描述
- 支持调整步骤顺序（上下移动）

### 2.3 中间：时间日历面板

#### 2.3.1 日期显示
- 顶部显示当前日期（如：2026年7月19日）
- 支持切换日期（前一天/后一天）
- 支持"回到今天"快捷操作

#### 2.3.2 时间线
- 垂直时间线，刻度从 00:00 到 24:00（全天 24 小时，每小时主刻度 + 每 30 分钟次刻度）
- 每 30 分钟一条次要刻度，每 1 小时一条主要刻度
- 时间线区域作为拖拽放置区

#### 2.3.3 已安排任务
- 以时间块形式展示已安排的任务
- 时间块显示：
  - 步骤标题
  - 开始时间
  - 结束时间
  - 持续时长
- 时间块支持：
  - 垂直拖拽调整时间
  - 上下边缘拖拽调整时长
  - 点击选中（右侧显示详情）
  - 从时间线拖出删除

#### 2.3.4 当前时间指示
- 显示一条红色/醒目的当前时间线
- 每分钟自动更新位置

### 2.4 右侧：任务详情面板

#### 2.4.1 默认状态
- 未选中任务时显示提示："选择一个步骤查看详情"

#### 2.4.2 详情展示
选中任务后显示：
- 步骤标题（可编辑）
- 所属父任务
- 执行标准/描述（多行文本，可编辑）
- 计划开始时间
- 计划结束时间
- 持续时长
- 完成状态勾选框

#### 2.4.3 操作按钮
- 保存修改
- 取消修改
- 删除该安排

### 2.5 顶部标题栏
- 产品标题"计划分解器"
- 导出日程按钮（导出为 JSON）

---

## 3. 交互需求

### 3.1 拖拽交互
- 左侧步骤卡片可拖拽
- 拖拽到中间时间线时，时间线高亮可放置区域
- 放置后自动计算合适的时间块长度
- 时间块内可再次拖拽调整位置

### 3.2 选中交互
- 点击时间线上的任务块，右侧显示详情
- 点击左侧步骤，右侧显示该步骤的原始信息
- 选中状态需有视觉反馈

### 3.3 AI 生成交互
- 点击"AI 分解"后显示加载状态
- 生成成功后平滑展示步骤列表
- 生成失败时显示错误提示

### 3.4 状态持久化
- 使用 localStorage 保存用户数据
- 刷新页面后数据不丢失

---

## 4. 非功能需求

### 4.1 性能
- 页面首屏加载时间 < 2s
- 拖拽操作流畅，帧率 > 30fps
- AI 生成有超时处理

### 4.2 可访问性
- 符合 WCAG 2.1 AA 标准
- 支持键盘操作
- 清晰的焦点状态
- 足够的颜色对比度

### 4.3 响应式
- 桌面端：完整三栏布局
- 平板端：可折叠侧边栏
- 移动端：优先显示时间线，侧栏通过抽屉展开

### 4.4 浏览器兼容
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

## 5. 视觉风格

### 5.1 设计理念
- 简洁、清晰、专注
- 柔和的色彩系统
- 充足的留白
- 清晰的视觉层次

### 5.2 色彩方向
- **单一主题**：仅提供浅色主题，不做深浅切换
- 主色：墨绿色系（#047857 / #065f46）
- 背景：浅灰/米白
- 强调色：温暖的橙色用于当前时间线
- 成功色：绿色
- 警告/错误：红色系

### 5.3 字体
- 中文：系统默认无衬线字体（PingFang SC, Microsoft YaHei）
- 英文/数字：Inter, system-ui
- 等宽字体用于时间显示

---

## 6. 数据模型

### 6.1 任务（Task）
```typescript
interface Task {
  id: string;
  title: string;
  description: string;
  createdAt: number;
  steps: Step[];
}
```

### 6.2 步骤（Step）
```typescript
interface Step {
  id: string;
  taskId: string;
  title: string;
  standard: string;      // 执行标准
  estimatedMinutes: number;
  order: number;
}
```

### 6.3 日程安排（ScheduleItem）
```typescript
interface ScheduleItem {
  id: string;
  stepId: string;
  date: string;          // YYYY-MM-DD
  startTime: string;     // HH:mm
  endTime: string;       // HH:mm
  completed: boolean;
  note?: string;
}
```

---

## 7. 成功指标

- 用户能顺利完成"输入计划 → AI 分解 → 拖拽排期 → 查看详情"的完整流程
- 拖拽操作无明显卡顿
- 数据持久化正常工作
- 在主流浏览器下视觉一致
